[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build:admin"
watchPatterns = ["packages/shared/src/**", "packages/admin-web/src/**"]

[deploy]
startCommand = "cd packages/admin-web && node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
