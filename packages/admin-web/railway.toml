[build]
builder = "NIXPACKS"
installCommand = "unset NPM_TOKEN && unset NPM_CONFIG_TOKEN && npm i"
buildCommand = "npm run build:shared && npm run build:admin"
watchPatterns = ["packages/shared/src/**", "packages/admin-web/src/**", "package.json", "package-lock.json", ".npmrc"]

[deploy]
startCommand = "cd packages/admin-web && node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
