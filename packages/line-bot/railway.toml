[build]
builder = "NIXPACKS"
buildCommand = "npm run build:shared && npm run build:bot"
watchPatterns = ["packages/shared/src/**", "packages/line-bot/src/**"]

[deploy]
startCommand = "cd packages/line-bot && node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
